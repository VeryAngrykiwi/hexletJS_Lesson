/* Задача
Реализуйте и экспортируйте по умолчанию функцию, которая принимает на вход количество минут (прошедших с начала суток) и возвращает строку, являющуюся временем в формате чч:мм. Если количество минут содержит 24 часа (1 сутки) либо больше, то функция возвращает время, прошедшее с полуночи последних суток.
*/

// ChatGPT
const formatTime = (minutes) => {
  // Проверка на валидность входных данных
  if (typeof minutes !== 'number' || minutes < 0) {
    return 'Неверный формат времени';
  }

  // Вычисляем часы и минуты
  const hours = Math.floor(minutes / 60) % 24;
  const mins = minutes % 60;

  // Форматируем результат
  const formattedHours = hours < 10 ? `0${hours}` : `${hours}`;
  const formattedMins = mins < 10 ? `0${mins}` : `${mins}`;

  return `${formattedHours}:${formattedMins}`;
};

// Пример использования:
const timeString = formatTime(1439); // 1439 минут = 23:59
console.log(timeString);

// replit 
const formattedTime = (minutes) => {
  // Вычисляем часы и минуты
  const hours = Math.floor(minutes / 60) % 24;
  const mins = minutes % 60;

  // Форматируем результат, используя padStart для добавления ведущего нуля
  return `${String(hours).padStart(2, '0')}:${String(mins).padStart(2, '0')}`;
};

console.log(formattedTime(5));

export default formattedTime;

/*Объяснения:
const hours = Math.floor(minutes / 60) % 24;: Эта строка вычисляет часы, разделив количество минут на 60 (получая общее количество часов) и затем взяв остаток от деления на 24 (чтобы получить часы в пределах суток).
const mins = minutes % 60;: Эта строка вычисляет минуты, беря остаток от деления на 60 (получая минуты в пределах часа).
return ${String(hours).padStart(2, '0')}:${String(mins).padStart(2, '0')};: Эта строка форматирует результат в строку в формате "чч:мм". String(hours) и String(mins) преобразуют числа часов и минут в строки. Метод padStart(2, '0') используется для добавления ведущего нуля, если число меньше 10, чтобы обеспечить двузначный формат для часов и минут.
console.log(formattedTime(5));: Пример использования функции с аргументом 5. В результате этого вызова функции будет выведена строка "00:05".
export default formattedTime;: Функция formattedTime экспортируется по умолчанию, что позволяет использовать ее в других файлах JavaScript, импортируя по ключевому слову import.
Таким образом, функция formattedTime принимает количество минут и возвращает строку в формате "чч:мм", с ведущими нулями для однозначных часов и минут.
*/